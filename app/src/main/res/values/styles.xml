<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <declare-styleable name="LeoCalendar">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="title_color" format="color" />
        <attr name="title_height" format="dimension"/>
        <attr name="title_text_color" format="color"/>
        <attr name="title_text_size" format="dimension"/>
        <attr name="month_leftArrow" format="reference"/>
        <attr name="month_rightArrow" format="reference"/>
        <attr name="year_leftArrow" format="reference"/>
        <attr name="year_rightArrow" format="reference"/>
    </declare-styleable>

</resources>
